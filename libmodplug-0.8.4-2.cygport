DESCRIPTION="MOD music library based on ModPlug"
HOMEPAGE="http://modplug-xmms.sourceforge.net/"
SRC_URI="mirror://sourceforge/modplug-xmms/${P}.tar.gz"

abi=0
PKG_NAMES="${PN} ${PN}${abi}"
PKG_HINTS="setup dll"
PKG_CONTENTS[0]='usr/include/ usr/lib/ usr/share/'
PKG_CONTENTS[1]="usr/bin/cygmodplug-${abi}.dll"

CYGCONF_ARGS="--enable-static"
